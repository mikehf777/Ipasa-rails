<%= form_for @rent_document, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :rent_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :rent_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :document_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :document_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :url, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :url, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                rent_documents_path, :class => 'btn' %>
  </div>
<% end %>
