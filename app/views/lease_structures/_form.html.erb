<%= form_for @lease_structure, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :structure_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :structure_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :cantidad, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :cantidad, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                lease_structures_path, :class => 'btn' %>
  </div>
<% end %>
